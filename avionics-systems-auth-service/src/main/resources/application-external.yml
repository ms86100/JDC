spring:
  config:
    activate:
      on-profile: external
  datasource:
    url: jdbc:postgresql://in0-eplmdb-v01:5432/systems
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: jira_auth
  flyway:
    enabled: true
    baseline-on-migrate: true
    default-schema: jira_auth
    schemas: jira_auth
    create-schemas: true
    validate-on-migrate: false
    locations: filesystem:migrations-external/avionics-systems-auth-service

logging:
  level:
    com.avionics_systems.auth: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
