spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jira_platform
    username: jiraadmin
    password: UNIpay@123

# Local dev: no SMTP server — mail health must not mark the service DOWN
management:
  health:
    mail:
      enabled: false