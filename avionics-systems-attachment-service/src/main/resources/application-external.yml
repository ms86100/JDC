spring:
  config:
    activate:
      on-profile: external
  datasource:
    url: jdbc:postgresql://in0-eplmdb-v01:5432/systems
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: jira_attachment
  flyway:
    enabled: true
    baseline-on-migrate: true
    default-schema: jira_attachment
    schemas: jira_attachment
    create-schemas: true
    validate-on-migrate: false
    locations: filesystem:migrations-external/avionics-systems-attachment-service

logging:
  level:
    com.avionics_systems.attachment: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
