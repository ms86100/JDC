// http-server storybook-static --port ${PORT:-3000} --silent
const { spawn } = require("child_process");
const port = process.env.PORT || 3000;
console.log("starting storybook at port:", port);
spawn("http-server", ["storybook-static", "--port", port, "--silent"]);
