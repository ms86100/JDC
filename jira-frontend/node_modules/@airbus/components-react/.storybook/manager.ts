import { addons } from '@storybook/manager-api';
import { create } from '@storybook/theming/create';
import packageJson from '../package.json';

addons.setConfig({
  theme: create({
    base: 'light',
    colorPrimary: "#002d80",
    // TODO use design system fonts
    // fontBase: '"Inter", sans-serif',
    // fontCode: '"Roboto Mono", monospace',
    brandTitle: `
       <span style="font-size:0.8rem;">Airbus Design System</span><br />
       <span style="font-size:0.8rem;font-weight:normal">React (${packageJson.version})<span>`,
    brandUrl: 'https://react.design-system.airbus.corp',
  }),
});