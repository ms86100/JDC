// @see https://create-react-app.dev/docs/running-tests/#srcsetuptestsjs
import "@testing-library/jest-dom";
import { toHaveNoViolations } from "jest-axe";

expect.extend(toHaveNoViolations);

beforeAll(() => {
  // @see https://jestjs.io/docs/jest-object#fake-timers
  jest.useFakeTimers({
    advanceTimers: true,
    // needed for testing library waitForElementToBeRemoved API
    // used only in Accordion tests currently
    doNotFake: ["setTimeout", "clearTimeout", "setInterval", "clearInterval"],
    now: new Date(2021, 5, 22),
  });
});
