import React from "react";
import { ColorModeProvider } from "../styling";

const withDarkMode = (Story: any) => (
  <ColorModeProvider mode="dark">
    <div className="bg-dark p-1">
      <Story />
    </div>
  </ColorModeProvider>
);

export default withDarkMode;
