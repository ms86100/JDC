export const mockBoundingClientRect = (
  element: HTMLElement,
  {
    left = 0,
    top = 0,
    right = 100,
    bottom = 16,
    width = 100,
    height = 16,
    x = 0,
    y = 0,
  }
) => {
  element.getBoundingClientRect = jest.fn(() => ({
    left,
    width,
    x,
    y,
    height,
    top,
    right,
    bottom,
    toJSON: () => ({}),
  }));
};
