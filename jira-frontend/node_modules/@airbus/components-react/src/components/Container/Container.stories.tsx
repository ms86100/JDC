import React from "react";
import Launch from "@material-design-icons/svg/filled/launch.svg?react";
import Button from "../Button";
import Stack from "../Stack";
import Container from "./Container";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof Container> = {
  id: "Components/Container",
  title: "General/Layout/Utilities/Container",
  component: Container,
};

export default meta;
type Story = StoryObj<typeof Container>;

export const Basic: Story = {
  tags: ["visual-test"],
  render: () => {
    const styles = {
      backgroundColor: "#e5eaf2",
      paddingTop: "16px",
      paddingBottom: "16px",
    };
    return (
      <Stack spacing="2-x">
        <Container style={styles}>Container fluid</Container>
        <Container until="xxl" style={styles}>
          Container fluid until "xxl"
        </Container>
        <Container until="xl" style={styles}>
          Container fluid until "xl"
        </Container>
        <Container until="lg" style={styles}>
          Container fluid until "lg"
        </Container>
        <Container until="md" style={styles}>
          Container fluid until "md"
        </Container>
        <Container until="sm" style={styles}>
          Container fluid until "sm"
        </Container>
        <Container until="xs" style={styles}>
          Container fluid until "xs"
        </Container>
        <Container until="xxs" style={styles}>
          Container fluid until "xxs"
        </Container>
        <Container fixed style={styles}>
          Container fixed
        </Container>
      </Stack>
    );
  },
};

export const Open: Story = {
  render: () => (
    <Button
      variant="primary"
      icon={<Launch />}
      onClick={() =>
        window.open("iframe.html?id=components-container--basic", "_blank")
      }
    >
      Open example
    </Button>
  ),
};
