import React from "react";
import Button from "../Button";
import Inline from "../Inline";
import Typography from "../Typography";
import Modal from "./Modal";
import { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof Modal> = {
  id: "Components/Modal",
  title: "Building Blocks/Modal",
  component: Modal,
  argTypes: {
    open: {
      control: false,
    },
  },
};

export default meta;
type Story = StoryObj<typeof Modal>;

export const Basic: Story = {
  tags: ["visual-test"],
  parameters: { docs: { source: { type: "code" } } },
  render: (args) => {
    const [open, setOpen] = React.useState(false);
    const handleOpen = () => {
      setOpen(true);
    };
    const handleClose = () => {
      setOpen(false);
    };
    return (
      <div>
        <Button onClick={handleOpen} aria-haspopup="dialog">
          Open modal
        </Button>
        <Modal
          {...args}
          aria-labelledby="modal-title"
          aria-describedby="modal-description"
          open={open}
          onClose={handleClose}
        >
          <div
            style={{
              position: "absolute",
              top: "50%",
              left: "50%",
              transform: "translate(-50%, -50%)",
              width: 400,
              height: 160,
              backgroundColor: "white",
              display: "flex",
              justifyContent: "center",
              alignItems: "center",
            }}
          >
            <Button onClick={handleClose}>Close modal</Button>
          </div>
        </Modal>
      </div>
    );
  },
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
  parameters: { docs: { source: { type: "code" } } },
  render: () => {
    const [open, setOpen] = React.useState(false);
    const handleOpen = () => {
      setOpen(true);
    };
    const handleClose = () => {
      setOpen(false);
    };
    return (
      <>
        <Button onClick={handleOpen} aria-haspopup="dialog">
          Open modal
        </Button>
        <Modal
          aria-labelledby="modal-title"
          aria-describedby="modal-description"
          open={open}
          onClose={handleClose}
        >
          <div
            style={{
              position: "absolute",
              top: "50%",
              left: "50%",
              transform: "translate(-50%, -50%)",
              width: 400,
              height: 160,
              backgroundColor: "#32353b",
              display: "flex",
              justifyContent: "center",
              alignItems: "center",
              boxShadow: "0 8px 32px 2px rgba(0, 0, 0, 0.20)",
            }}
          >
            <Button onClick={handleClose}>Close modal</Button>
          </div>
        </Modal>
      </>
    );
  },
};

export const Focus: Story = {
  parameters: { docs: { source: { type: "code" } } },
  render: () => {
    const [open, setOpen] = React.useState(false);
    const handleOpen = () => {
      setOpen(true);
    };
    const handleClose = () => {
      setOpen(false);
    };
    return (
      <div>
        <Button onClick={handleOpen} aria-haspopup="dialog">
          Open modal
        </Button>
        <Modal
          aria-labelledby="modal-title"
          aria-describedby="modal-description"
          open={open}
          onClose={handleClose}
        >
          <div
            tabIndex={-1}
            style={{
              position: "absolute",
              top: "50%",
              left: "50%",
              transform: "translate(-50%, -50%)",
              width: 400,
              height: 160,
              backgroundColor: "white",
              padding: "1rem",
              overflow: "auto",
              outline: 0,
            }}
          >
            <Typography variant="h1">Modal</Typography>
            <p>
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Aut,
              deserunt eaque tempore accusantium eligendi veniam exercitationem
              quaerat magni voluptates dolorem, quo sapiente voluptatibus
              consequuntur. Ab voluptatem similique cumque earum itaque?
            </p>
            <p>
              Lorem ipsum dolor sit amet consectetur adipisicing elit. Placeat
              in provident et, culpa, quae nostrum cupiditate qui natus quia
              delectus eveniet distinctio numquam! Sit earum error velit modi
              deserunt ipsa.
            </p>
            <Inline spacing="2-x">
              <Button onClick={handleClose}>CANCEL</Button>
              <Button tabIndex={1} variant="primary" onClick={handleClose}>
                OK
              </Button>
            </Inline>
          </div>
        </Modal>
      </div>
    );
  },
};
