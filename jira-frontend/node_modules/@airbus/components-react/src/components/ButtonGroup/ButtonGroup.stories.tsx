import React from "react";
import { Info } from "@airbus/icons/react";
import ViewHeadline from "@material-design-icons/svg/filled/view_headline.svg?react";
import ViewModule from "@material-design-icons/svg/filled/view_module.svg?react";
import Button from "../Button";
import Tooltip from "../Tooltip";
import IconButton from "../IconButton";
import Inline from "../Inline";
import Stack from "../Stack";
import ButtonGroup from "./ButtonGroup";
import type { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof ButtonGroup> = {
  id: "Components/ButtonGroup",
  title: "Components/Button Group",
  component: ButtonGroup,
  render: (args) => (
    <ButtonGroup aria-label="Numberd Sections" {...args}>
      <Button pressed>One</Button>
      <Button>Two</Button>
      <Button disabled>Three</Button>
    </ButtonGroup>
  ),
};

export default meta;
type Story = StoryObj<typeof ButtonGroup>;

export const Basic: Story = {
  tags: ["visual-test"],
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
};

export const Icons: Story = {
  render: () => (
    <Inline spacing="2-x">
      <ButtonGroup aria-label="Display modes">
        <Button>
          <ViewHeadline />
          List
        </Button>
        <Button>
          <ViewModule />
          Grid
        </Button>
      </ButtonGroup>
      <ButtonGroup aria-label="Display modes">
        <IconButton>
          <ViewHeadline />
        </IconButton>
        <IconButton>
          <ViewModule />
        </IconButton>
      </ButtonGroup>
    </Inline>
  ),
};

export const Sizes: Story = {
  render: () => (
    <Stack spacing="3-x">
      <Inline spacing="2-x">
        <ButtonGroup size="large" aria-label="Display modes">
          <Button>
            <ViewHeadline />
            List
          </Button>
          <Button>
            <ViewModule />
            Grid
          </Button>
        </ButtonGroup>
        <ButtonGroup size="large" aria-label="Display modes">
          <IconButton>
            <ViewHeadline />
          </IconButton>
          <IconButton>
            <ViewModule />
          </IconButton>
        </ButtonGroup>
      </Inline>
      <Inline spacing="2-x">
        <ButtonGroup aria-label="Display modes">
          <Button>
            <ViewHeadline />
            List
          </Button>
          <Button>
            <ViewHeadline />
            Second list
          </Button>
          <Button>
            <ViewModule />
            Grid
          </Button>
        </ButtonGroup>
        <ButtonGroup aria-label="Display modes">
          <IconButton>
            <ViewHeadline />
          </IconButton>
          <IconButton>
            <ViewModule />
          </IconButton>
        </ButtonGroup>
      </Inline>
      <Inline spacing="2-x">
        <ButtonGroup size="small" aria-label="Display modes">
          <Button>
            <ViewHeadline />
            List
          </Button>
          <Button>
            <ViewModule />
            Grid
          </Button>
        </ButtonGroup>
        <ButtonGroup size="small" aria-label="Display modes">
          <IconButton>
            <ViewHeadline />
          </IconButton>
          <IconButton>
            <ViewModule />
          </IconButton>
        </ButtonGroup>
      </Inline>
    </Stack>
  ),
};

export const Custom: Story = {
  parameters: { docs: { source: { type: "code" }}},
  render: () => {
    const IconButtonWithTooltip = ({ children, placement }: any) => {
      return (
        <Tooltip placement={placement} label={placement}>
          <IconButton size="large" variant="ghost">
            {children}
          </IconButton>
        </Tooltip>
      );
    };
    return (
      <ButtonGroup size="large">
        <IconButtonWithTooltip placement="top">
          <ViewHeadline />
        </IconButtonWithTooltip>
        <IconButtonWithTooltip placement="right">
          <ViewModule />
        </IconButtonWithTooltip>
        <IconButtonWithTooltip placement="bottom">
          <Info />
        </IconButtonWithTooltip>
      </ButtonGroup>
    );
  },
};
