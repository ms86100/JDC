import { Meta, Primary, Controls, Source, Canvas } from '@storybook/blocks';
import * as StackStories from "./Stack.stories";

<Meta of={StackStories} />

# Stack

The `Stack` component allows you to render a set of components in a column with equal spacing around them.

<Source code="import { Stack } from &#x22;@airbus/components-react&#x22;" />

<Primary />

<Controls />

## Examples

### Spacing

<Canvas of={StackStories.SetSpacing} />

### Dividers

<Canvas of={StackStories.Dividers} />
