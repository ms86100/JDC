import React from "react";
import Placeholder from "../internal/Placeholder";
import Flex from "./Flex";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof Flex> = {
  id: "Components/Flex",
  title: "General/Layout/Utilities/Flex",
  component: Flex,
  argTypes: {
    childrenStyle: { control: false },
    spacing: { control: false },
  },
};

export default meta;
type Story = StoryObj<typeof Flex>;

export const Basic: Story = {
  tags: ["visual-test"],
  args: {
    justify: "space-between",
    alignItems: "center",
  },
  render: (args) => (
    <Flex {...args}>
      {Array.from({ length: 6 }).map((item, i) => (
        <Placeholder key={i} height={48 + i * 16} width={48 + i * 16} />
      ))}
    </Flex>
  ),
};

export const Wrap: Story = {
  render: () => (
    <Flex wrap gap="1-x">
      {Array.from({ length: 32 }).map((item, i) => (
        <Placeholder key={i} />
      ))}
    </Flex>
  ),
};
