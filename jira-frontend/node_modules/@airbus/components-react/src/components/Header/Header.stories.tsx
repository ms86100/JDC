import React from "react";
import Help from "@material-design-icons/svg/filled/help.svg?react";
import Notifications from "@material-design-icons/svg/filled/notifications.svg?react";
import Search from "@material-design-icons/svg/filled/search.svg?react";
import Button from "../Button"
import IconButton from "../IconButton";
import Tabs, { Tab } from "../Tabs";
import Header from "./Header";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof Header> = {
  id: "Components/Header",
  title: "Components/Header",
  component: Header,
};

export default meta;
type Story = StoryObj<typeof Header>;

export const Basic: Story = {
  tags: ["visual-test"],
  args: { appName: "App Name" },
  render: (args) => (
    <Header {...args}>
      <Tabs value={0}>
        <Tab value={0}>Tab Label</Tab>
        <Tab value={1}>Tab Label</Tab>
        <Tab value={2}>Tab Label</Tab>
      </Tabs>
    </Header>
  ),
};

export const Dark: Story = {
  tags: ["visual-test"],
  render: () => (
    <Header appName="App Name" theme="dark">
      <Tabs value={0}>
        <Tab value={0}>Tab Label</Tab>
        <Tab value={1}>Tab Label</Tab>
        <Tab value={2}>Tab Label</Tab>
      </Tabs>
    </Header>
  ),
};

export const HomeNavigation: Story = {
  render: () => (
    <Header appName="App Name" onHomeClick={() => console.log("Home clicked")}>
      <Tabs value={0}>
        <Tab value={0}>Tab Label</Tab>
        <Tab value={1}>Tab Label</Tab>
        <Tab value={2}>Tab Label</Tab>
      </Tabs>
    </Header>
  ),
};

export const Actions: Story = {
  render: () => (
    <Header appName="App Name">
      <IconButton variant="ghostNegative" aria-label="Search">
        <Search />
      </IconButton>
      <IconButton variant="ghostNegative" aria-label="Notifications">
        <Notifications />
      </IconButton>
      <IconButton variant="ghostNegative" aria-label="Help">
        <Help />
      </IconButton>
      <Button variant="secondary">Contact</Button>
    </Header>
  ),
};

export const Full: Story = {
  render: () => (
    <Header appName="App Name">
      <Tabs value={0}>
        <Tab value={0}>Tab Label</Tab>
        <Tab value={1}>Tab Label</Tab>
        <Tab value={2}>Tab Label</Tab>
      </Tabs>
      <IconButton variant="ghostNegative" aria-label="Search">
        <Search />
      </IconButton>
      <IconButton variant="ghostNegative" aria-label="Notifications">
        <Notifications />
      </IconButton>
      <IconButton variant="ghostNegative" aria-label="Help">
        <Help />
      </IconButton>
      <Button variant="secondary">Contact</Button>
    </Header>
  ),
};

export const Responsive: Story = {
  render: () => (
    <div style={{ width: "480px" }}>
      <Header
        appName="App Name"
        showMenuButton
        onMenuButtonClick={() => console.log("Menu button clicked")}
      />
    </div>
  ),
};
