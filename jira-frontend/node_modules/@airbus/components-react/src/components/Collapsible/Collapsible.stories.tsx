import React from "react";
import type { Meta, StoryObj } from "@storybook/react";
import Chip from "../Chip";
import ComboBox from "../ComboBox";
import Flex from "../Flex";
import { FormControl } from "../Form";
import Slider from "../Slider";
import Stack from "../Stack";
import Collapsible from "./Collapsible";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof Collapsible> = {
  component: Collapsible,
  id: "Components/Collapsible",
  title: "Building Blocks/Collapsible",
  args: {
    title: "Title",
    expanded: true,
  },
  render: (args) => {
    return <Collapsible {...args}>Expanded content</Collapsible>;
  },
};

export default meta;
type Story = StoryObj<typeof Collapsible>;

export const Basic: Story = {
  tags: ["visual-test"],
};

export const Disabled: Story = {
  render: () => (
    <div className="w-480">
      <Collapsible title="Title" disabled>
        Expanded content
      </Collapsible>
    </div>
  ),
};

export const FilterPanel: Story = {
  render: () => (
    <div className="w-208">
      <Collapsible title="Main filters" variant="filterpanel" defaultExpanded>
        <Stack spacing="1-x">
          <ComboBox placeholder="Category" />
          <ComboBox placeholder="Subcategory" />
        </Stack>
      </Collapsible>
      <Collapsible title="Ranges" variant="filterpanel">
        <FormControl label="Quantity" required>
          <Slider
            ticks={[0, 100, 200]}
            min={0}
            max={200}
            defaultValue={[0, 200]}
          />
        </FormControl>
        <FormControl label="Price ($)" required>
          <Slider min={0} max={2000} defaultValue={[0, 2000]} ticks={3} />
        </FormControl>
      </Collapsible>
    </div>
  ),
};

export const CustomTitle: Story = {
  parameters: { docs: { source: { type: "code" }}},
  render: () => {
    const Title = () => (
      <Flex justify="space-between" style={{ width: "410px" }}>
        <span>48002 / AC - MCU</span>
        <Flex gap="1-x">
          <Chip>A320 - 200</Chip>
          <Chip variant="success">5</Chip>
          <Chip variant="error">1</Chip>
        </Flex>
      </Flex>
    );
    return (
      <div className="w-480">
        <Collapsible title={<Title />}>Expanded content</Collapsible>
      </div>
    );
  },
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
  render: () => (
      <div className="w-480">
        <Collapsible title="Title">Expanded content</Collapsible>
        <br />
        <Collapsible title="Title" variant="filterpanel">
          Expanded content
        </Collapsible>
      </div>
  ),
};
