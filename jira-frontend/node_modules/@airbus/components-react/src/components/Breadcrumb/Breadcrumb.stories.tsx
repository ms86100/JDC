import React from "react";
import { MoreHoriz } from "@airbus/icons/react";
import IconButton from "../IconButton";
import Link from "../Link";
import Menu, { MenuButton, MenuItem, MenuList } from "../Menu";
import Breadcrumb, { BreadcrumbItem } from ".";
import { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof Breadcrumb> = {
  id: "Components/Breadcrumb",
  title: "Components/Breadcrumb",
  component: Breadcrumb,
  render: (args) => (
    <Breadcrumb {...args}>
      <BreadcrumbItem>
        <Link href="#" onClick={(e) => e.preventDefault()}>
          Home
        </Link>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <Link href="#" onClick={(e) => e.preventDefault()}>
          A320 Familly
        </Link>
      </BreadcrumbItem>
      <BreadcrumbItem current>A320neo</BreadcrumbItem>
    </Breadcrumb>
  ),
};

export default meta;
type Story = StoryObj<typeof Breadcrumb>;

export const Basic: Story = {
  tags: ["visual-test"],
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
};

export const Sizes: Story = {
  render: () => (
    <>
      <Breadcrumb size="small">
        <BreadcrumbItem>
          <Link href="#" onClick={(e) => e.preventDefault()}>
            Home
          </Link>
        </BreadcrumbItem>
        <BreadcrumbItem>
          <Link href="#" onClick={(e) => e.preventDefault()}>
            A320 Familly
          </Link>
        </BreadcrumbItem>
        <BreadcrumbItem current>A320neo</BreadcrumbItem>
      </Breadcrumb>
      <Breadcrumb size="medium">
        <BreadcrumbItem>
          <Link href="#" onClick={(e) => e.preventDefault()}>
            Home
          </Link>
        </BreadcrumbItem>
        <BreadcrumbItem>
          <Link href="#" onClick={(e) => e.preventDefault()}>
            A320 Familly
          </Link>
        </BreadcrumbItem>
        <BreadcrumbItem current>A320neo</BreadcrumbItem>
      </Breadcrumb>
    </>
  ),
};

export const Overflow: Story = {
  parameters: {
    docs: {
      source: {
        type: "code",
        // adding this code additionally, since component={IconButton} will be replaced by react code in documentation.
        code: `
<Breadcrumb>
  <BreadcrumbItem>
    <Link href="#" onClick={(e) => e.preventDefault()}>
      Products & Services
    </Link>
  </BreadcrumbItem>
  <BreadcrumbItem>
    <Menu>
      <MenuButton variant="ghost" size="small" component={IconButton}>
        <MoreHoriz />
      </MenuButton>
      <MenuList>
        <MenuItem onClick={() => {}}>Commercial Aircraft</MenuItem>
        <MenuItem onClick={() => {}}>Passenger Aircraft</MenuItem>
      </MenuList>
    </Menu>
  </BreadcrumbItem>
  <BreadcrumbItem>
    <Link href="#" onClick={(e) => e.preventDefault()}>
      A320 Familly
    </Link>
  </BreadcrumbItem>
  <BreadcrumbItem current>A320neo</BreadcrumbItem>
</Breadcrumb>
        `,
      },
    },
  },
  render: () => (
    <Breadcrumb>
      <BreadcrumbItem>
        <Link href="#" onClick={(e) => e.preventDefault()}>
          Products & Services
        </Link>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <Menu>
          <MenuButton variant="ghost" size="small" component={IconButton}>
            <MoreHoriz />
          </MenuButton>
          <MenuList>
            <MenuItem onClick={() => {}}>Commercial Aircraft</MenuItem>
            <MenuItem onClick={() => {}}>Passenger Aircraft</MenuItem>
          </MenuList>
        </Menu>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <Link href="#" onClick={(e) => e.preventDefault()}>
          A320 Familly
        </Link>
      </BreadcrumbItem>
      <BreadcrumbItem current>A320neo</BreadcrumbItem>
    </Breadcrumb>
  ),
};
