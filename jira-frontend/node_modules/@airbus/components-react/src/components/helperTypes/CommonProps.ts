import { CSSProperties } from "react";

/**
 * Common props for all components
 * [key: string] syntax allow to support any rest props
 */
export type CommonProps = {
  className?: string;
  style?: CSSProperties;
  [key: string]: any;
};
