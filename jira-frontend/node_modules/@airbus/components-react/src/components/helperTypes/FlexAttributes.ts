export type AlignItems = "start" | "center" | "end" | "baseline" | "stretch";
export type AlignSelf = "start" | "center" | "end" | "baseline" | "stretch";
export type FlexDirection = "row" | "row-reverse" | "column" | "column-reverse";
export type JustifyContent =
  | "start"
  | "center"
  | "end"
  | "space-around"
  | "space-between"
  | "space-evenly";

// FIXME would not need to duplicate the infos with enums instead union types
export const AlignItemsValues: AlignItems[] = [
  "start",
  "center",
  "end",
  "baseline",
  "stretch",
];

export const JustifyContentValues: JustifyContent[] = [
  "start",
  "center",
  "end",
  "space-around",
  "space-between",
  "space-evenly",
];
