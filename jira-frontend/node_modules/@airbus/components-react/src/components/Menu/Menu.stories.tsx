import React from "react";
import type { Meta, StoryObj } from "@storybook/react";
import MoreVert from "@material-design-icons/svg/filled/more_vert.svg?react";
import GetApp from "@material-design-icons/svg/filled/get_app.svg?react";
import Print from "@material-design-icons/svg/filled/print.svg?react";
import Refresh from "@material-design-icons/svg/filled/refresh.svg?react";
import withDarkMode from "../../lib/utils/storybookDecorators";
import IconButton from "../IconButton";
import Menu, { MenuButton, MenuList, MenuItem, MenuDivider } from ".";

const meta: Meta<typeof Menu> = {
  id: "Components/Menu",
  title: "Components/Overflow Menu",
  component: Menu,
  subcomponents: {
    MenuButton: MenuButton as React.ComponentType<unknown>,
    MenuList: MenuList as React.ComponentType<unknown>,
    MenuItem: MenuItem as React.ComponentType<unknown>,
    MenuDivider: MenuDivider as React.ComponentType<unknown>,
  },
};

export default meta;
type Story = StoryObj<typeof Menu>;

export const Basic: Story = {
  tags: ["visual-test"],
  render: (args) => (
    <Menu {...args}>
      <MenuButton variant="ghost">Open Menu</MenuButton>
      <MenuList>
        <MenuItem>View in Full screen</MenuItem>
        <MenuItem onClick={() => {}}>View Data Table</MenuItem>
        <MenuDivider />
        <MenuItem onClick={() => {}} icon={<Print />}>
          Print
        </MenuItem>
        <MenuItem disabled onClick={() => {}} icon={<GetApp />}>
          Download
        </MenuItem>
        <MenuItem onClick={() => {}} icon={<Refresh />}>
          Refresh
        </MenuItem>
      </MenuList>
    </Menu>
  ),
};

export const Dark: Story = {
  tags: ["visual-test"],
  ...Basic,
  decorators: [withDarkMode],
};

export const WithIconButton: Story = {
  parameters: {
    docs: {
      source: {
        type: "code",
        // adding this code additionally, since component={IconButton} will be replaced by react code in documentation.
        code: `
<Menu>
  <MenuButton variant="ghost" size="small" component={IconButton}>
    <MoreVert />
  </MenuButton>
  <MenuList>
    <MenuItem onClick={() => {}}>View in Full screen</MenuItem>
    <MenuItem onClick={() => {}}>View Data Table</MenuItem>
    <MenuDivider />
    <MenuItem onClick={() => {}} icon={<Print />}>
      Print
    </MenuItem>
    <MenuItem disabled onClick={() => {}} icon={<GetApp />}>
      Download
    </MenuItem>
    <MenuItem onClick={() => {}} icon={<Refresh />}>
      Refresh
    </MenuItem>
  </MenuList>
</Menu>
    `,
      },
    },
  },
  render: () => (
    <Menu>
      <MenuButton variant="ghost" size="small" component={IconButton}>
        <MoreVert />
      </MenuButton>
      <MenuList>
        <MenuItem onClick={() => {}}>View in Full screen</MenuItem>
        <MenuItem onClick={() => {}}>View Data Table</MenuItem>
        <MenuDivider />
        <MenuItem onClick={() => {}} icon={<Print />}>
          Print
        </MenuItem>
        <MenuItem disabled onClick={() => {}} icon={<GetApp />}>
          Download
        </MenuItem>
        <MenuItem onClick={() => {}} icon={<Refresh />}>
          Refresh
        </MenuItem>
      </MenuList>
    </Menu>
  ),
};
