import { Meta, Primary, Controls, Source, Canvas } from '@storybook/blocks';
import LinkTo from "@storybook/addon-links/react";
import * as MenuStories from "./Menu.stories";

<Meta of={MenuStories} />

# Overflow Menu

Display additional links or options when the space is limited.
See [Overflow Menu guidelines<i className="ds-icon-launch"></i>](https://design-system.airbus.corp/components/overflow-menu/design/)

<Source code="import { Menu, MenuButton, MenuItem, MenuList } from &#x22;@airbus/components-react&#x22;" />

<Primary />

<Controls />

* Any other props supplied will be provided to the root elements

## Examples

### Dark Mode

<Canvas of={MenuStories.Dark} />

Details about implementing dark mode is available in <LinkTo kind="General/Visual Styles/Introduction" story="docs">Visual styles Page</LinkTo>

### With IconButton

Use the MenuButton `component` prop to display an `IconButton` instead of a `Button`.

<Canvas of={MenuStories.WithIconButton} />