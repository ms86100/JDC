import React from "react";
import Button from "../Button";
import Flex from "../Flex";
import Inline from "../Inline";
import Toggle from "../Toggle";
import Tooltip, { TooltipDelay } from "./Tooltip";
import type { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof Tooltip> = {
  id: "Components/Tooltip",
  title: "Components/Tooltip",
  component: Tooltip,
  argTypes: {
    arrowClassName: { control: false },
    arrowSize: { control: false },
    enterDelay: { options: [0, 300, 500] },
    exitDelay: { options: [0, 300, 500] },
  },
};

export default meta;
type Story = StoryObj<typeof Tooltip>;

export const Basic: Story = {
  tags: ["visual-test"],
  args: {
    placement: "left",
    label: "Lorem ipsum",
  },
  render: (args) => (
    <Inline align="center" spacing="2-x">
      <Tooltip {...args}>
        <Button variant="ghost">Hover me</Button>
      </Tooltip>
    </Inline>
  ),
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
  render: () => (
    <Inline align="center" spacing="2-x">
      <Tooltip placement="left" label="Lorem ipsum">
        <Button variant="ghost">Left</Button>
      </Tooltip>
      <Tooltip placement="top" label="Lorem ipsum">
        <Button variant="ghost">Top</Button>
      </Tooltip>
      <Tooltip placement="bottom" label="Lorem ipsum">
        <Button variant="ghost">Bottom</Button>
      </Tooltip>
      <Tooltip placement="right" label="Lorem ipsum">
        <Button variant="ghost">Right</Button>
      </Tooltip>
    </Inline>
  ),
};

export const Large: Story = {
  render: () => (
    <Inline align="center" spacing="2-x">
      <Tooltip
        placement="left"
        large
        title="Title"
        label={
          <div>
            00% lorem ipsum
            <br />
            00% lorem ipsum
          </div>
        }
      >
        <Button variant="ghost">Left</Button>
      </Tooltip>
      <Tooltip
        placement="top"
        large
        title="Title"
        label={
          <div>
            00% lorem ipsum
            <br />
            00% lorem ipsum
          </div>
        }
      >
        <Button variant="ghost">Top</Button>
      </Tooltip>
      <Tooltip
        placement="bottom"
        large
        title="Title"
        label={
          <div>
            00% lorem ipsum
            <br />
            00% lorem ipsum
          </div>
        }
      >
        <Button variant="ghost">Bottom</Button>
      </Tooltip>
      <Tooltip
        placement="right"
        large
        title="Title"
        label={
          <div>
            00% lorem ipsum
            <br />
            00% lorem ipsum
          </div>
        }
      >
        <Button variant="ghost">Right</Button>
      </Tooltip>
    </Inline>
  ),
};

export const Disabled: Story = {
  parameters: { docs: { source: { type: "code" }}},
  render: () => {
    const [disabled, setDisabled] = React.useState(false);
    return (
      <Inline align="center" spacing="2-x">
        <Tooltip placement="left" label="Lorem ipsum" disabled={disabled}>
          <Button variant="ghost">Hover me</Button>
        </Tooltip>
        <Toggle
          label="Disabled"
          checked={disabled}
          onChange={() => setDisabled(!disabled)}
        />
      </Inline>
    );
  },
};

export const Styling: Story = {
  render: () => (
    <Flex style={{ margin: "auto" }}>
      <Tooltip
        placement="bottom"
        label="Lorem ipsum"
        className="custom-tooltip"
        arrowClassName="custom-tooltip-arrow"
        targetClassName="custom-tooltip-target"
      >
        <Button variant="ghost">Styled Tooltip</Button>
      </Tooltip>
    </Flex>
  ),
};

export const Delay: Story = {
  render: () => (
    <Inline align="center" spacing="2-x">
      <Tooltip label="Lorem ipsum">
        <Button variant="ghost">Default</Button>
      </Tooltip>
      <Tooltip
        label="Lorem ipsum"
        enterDelay={TooltipDelay.NONE}
        exitDelay={TooltipDelay.NONE}
      >
        <Button variant="ghost">None</Button>
      </Tooltip>
      <Tooltip
        label="Lorem ipsum"
        enterDelay={TooltipDelay.LONG}
        exitDelay={TooltipDelay.NONE}
      >
        <Button variant="ghost">Long enter</Button>
      </Tooltip>
      <Tooltip
        label="Lorem ipsum"
        enterDelay={TooltipDelay.NONE}
        exitDelay={TooltipDelay.MEDIUM}
      >
        <Button variant="ghost">Long exit</Button>
      </Tooltip>
      <Tooltip
        label="Lorem ipsum"
        enterDelay={TooltipDelay.LONG}
        exitDelay={TooltipDelay.LONG}
      >
        <Button variant="ghost">Both</Button>
      </Tooltip>
    </Inline>
  ),
};
