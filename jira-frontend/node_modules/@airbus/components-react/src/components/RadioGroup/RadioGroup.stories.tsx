import React, { useState } from "react";
import Radio from "../Radio";
import RadioGroup from "./RadioGroup";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof RadioGroup> = {
  id: "Components/RadioGroup",
  title: "Components/Radio Button/Radio Group",
  component: RadioGroup,
};

export default meta;
type Story = StoryObj<typeof RadioGroup>;

export const Basic: Story = {
  tags: ["visual-test"],
  args: {
    defaultValue: "318",
  },
  render: (args) => (
    <RadioGroup {...args}>
      <Radio value="318" label="A318" />
      <Radio value="319" label="A319" />
      <Radio value="320" label="A320" />
      <Radio value="321" label="A321" />
      <Radio value="330" label="A330" />
      <Radio value="350" label="A350" />
    </RadioGroup>
  ),
};

export const Controlled: Story = {
  parameters: { docs: { source: { type: "code" }}},
  render: () => {
    const [selectedValue, setSelectedValue] = useState("318");
    return (
      <RadioGroup
        value={selectedValue}
        onChange={(e, value) => setSelectedValue(value)}
      >
        <Radio value="318" label="A318" />
        <Radio value="319" label="A319" />
        <Radio value="320" label="A320" />
        <Radio value="321" label="A321" />
        <Radio value="330" label="A330" />
        <Radio value="350" label="A350" />
      </RadioGroup>
    );
  },
};
