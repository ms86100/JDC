import { Meta, Primary, Controls, Source, Canvas } from '@storybook/blocks';
import * as InlineStories from "./Inline.stories";

<Meta of={InlineStories} />
# Inline

The `Inline` component allows you to render a set of components in a row with equal spacing around them.

<Source code="import { Inline } from &#x22;@airbus/components-react&#x22;" />

<Primary />

<Controls />

### Spacing

<Canvas of={InlineStories.SetSpacing} />

### Overflow

Scroll behaviour can be removed with an `overflow: "hidden"`.

<Canvas of={InlineStories.NoScroll} />

Or use the `wrap` prop to enable automatic wrapping on multipline lines.

<Canvas of={InlineStories.Wrap} />

### Align Horizontally

<Canvas of={InlineStories.AlignHoriz} />

### Align Vertically

<Canvas of={InlineStories.AlignVert} />
