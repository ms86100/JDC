import React from "react";
import Divider from "./Divider";
import type { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";
import Typography from "../Typography";

const meta: Meta<typeof Divider> = {
  id: "Components/Divider",
  title: "Building Blocks/Divider",
  component: Divider,
  render: (args) => <Divider {...args} />,
  argTypes: {
    component: {
      control: { type: "select" },
      options: ["hr", "div", "span", "p"],
      description:
        "Specifies the component to be used as the root node. The options provided here are only examples, you can use any valid React element type.",
    },
  },
};

export default meta;

type Story = StoryObj<typeof Divider>;

export const Basic: Story = {
  tags: ["visual-test"],
  render: (args) => <Divider {...args} />,
};

export const Vertical: Story = {
  render: () => (
    <div style={{ display: "flex" }}>
      <span>Foo</span>
      <Divider vertical />
      <span>Bar</span>
    </div>
  ),
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
  render: () => (
    <div className="bg-dark p-1">
      <Divider />
      <br />
      <div style={{ display: "flex" }}>
        <Typography>Foo</Typography>
        <Divider vertical />
        <Typography>Bar</Typography>
      </div>
    </div>
  ),
};

export const Inset: Story = {
  render: () => (
    <>
      <span>Without inset</span>
      <Divider />
      <span>With inset</span>
      <Divider inset />
    </>
  ),
};
