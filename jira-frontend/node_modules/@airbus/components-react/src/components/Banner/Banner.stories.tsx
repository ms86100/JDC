import React from "react";
import Inline from "../Inline";
import Stack from "../Stack";
import Banner from "./Banner";
import { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";
import Link from "../Link";

const meta: Meta<typeof Banner> = {
  id: "Components/Banner",
  title: "Components/Banner",
  component: Banner,
  render: (args) => (
    <Banner {...args} onClose={() => {}}>
      Airbus uses cookies to enhance your browsing experience.
    </Banner>
  ),
  argTypes: {
    variant: {
      options: ["info", "warning", "error", "success"],
      control: "radio",
    },
  },
};

export default meta;

type Story = StoryObj<typeof Banner>;

export const Basic: Story = {
  tags: ["visual-test"],
};

export const Variants: Story = {
  tags: ["visual-test"],
  render: () => {
    return (
      <Stack spacing="2-x" wrapChildren>
        <Banner onClose={() => {}}>
          <Inline align="space-between" style={{ width: "100%" }}>
            <span>Airbus uses cookies to enhance your browsing experience.</span>
            <Link negative size="small" href="#" target="_blank">
              I agree
            </Link>
          </Inline>
        </Banner>
        <Banner variant="success" onClose={() => {}}>
          <Inline align="space-between" style={{ width: "100%" }}>
            <span>Your message has been sent. We will come back to you as soon as
            possible.</span>
            <Link negative size="small" href="#" target="_blank">
              Learn more
            </Link>
          </Inline>
        </Banner>
        <Banner variant="warning" onClose={() => {}}>
          <Inline align="space-between" style={{ width: "100%" }}>
            <span>We are updating the platform. Your experience can be slower during
            the next hour.</span>
            <Link negative  size="small" href="#" target="_blank">
              See message
            </Link>
          </Inline>
        </Banner>
        <Banner variant="error" onClose={() => {}}>
          <Inline align="space-between" style={{ width: "100%" }}>
            <span>No internet connection. You need to be online to perform this task.</span>
            <Link negative  size="small" href="#" target="_blank">
              Refresh
            </Link>
          </Inline>
        </Banner>
      </Stack>
    );
  },
};

export const Dark: Story = {
  tags: ["visual-test"],
  ...Variants,
  decorators: [withDarkMode],
};
