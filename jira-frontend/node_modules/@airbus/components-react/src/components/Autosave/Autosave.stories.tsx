import React from "react";
import { ColorModeProvider } from "../../lib/styling";
import Inline from "../Inline";
import Stack from "../Stack";
import Autosave from "./";
import type { Meta, StoryObj } from "@storybook/react";
import withDarkMode from "../../lib/utils/storybookDecorators";

const meta: Meta<typeof Autosave> = {
  title: "Components/Autosave",
  component: Autosave,
};
export default meta;
type Story = StoryObj<typeof Autosave>;

export const Basic: Story = {
  tags: ["visual-test"],
  render: (args) => {
    return <Autosave {...args} />;
  },
};

export const Size: Story = {
  render: () => {
    return (
      <Stack spacing="half-x">
        <Inline spacing="2-x" alignY="end">
          <Autosave size="small" />
          <Autosave size="small" saved />
        </Inline>
        <Inline spacing="2-x" alignY="end">
          <Autosave />
          <Autosave saved />
        </Inline>
      </Stack>
    );
  },
};

export const Dark: Story = {
  tags: ["visual-test"],
  decorators: [withDarkMode],
  render: () => {
    return (
      <Inline spacing="2-x" className="bg-dark p-1">
        <Autosave />
        <Autosave saved />
      </Inline>
    );
  },
};

export const Label: Story = {
  render: () => {
    return (
      <Inline spacing="2-x">
        <Autosave>Checking connection...</Autosave>
        <Autosave saved>Connected</Autosave>
      </Inline>
    );
  },
};
