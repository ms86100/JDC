import * as React from "react";
import { render, screen } from "@testing-library/react";
import { composeStories } from "@storybook/react";
import { axe } from "jest-axe";
import * as stories from "./Autosave.stories";
import Autosave from ".";

const { Basic } = composeStories(stories) as any;

describe("Autosave component", () => {
  it("pass A11Y", async () => {
    const { container } = render(<Basic />);
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });

  it("passes through props", () => {
    render(<Autosave data-testid="autosave" />);
    expect(screen.getByRole("status")).toHaveAttribute("data-testid", "autosave");
  });
});
