import React from "react";
import type { Meta, StoryObj } from "@storybook/react";
import Launch from "@material-design-icons/svg/filled/launch.svg?react";
import withDarkMode from "../../lib/utils/storybookDecorators";
import Divider from "../Divider";
import List, { ListItem, ListItemText, ListItemIcon } from ".";

const meta: Meta<typeof List> = {
  id: "Components/List",
  title: "Building Blocks/List",
  component: List,
  subcomponents: {
    ListItem: ListItem as React.ComponentType<unknown>,
    ListItemText: ListItemText as React.ComponentType<unknown>,
    ListItemIcon: ListItemIcon as React.ComponentType<unknown>,
  },
  argTypes: {
    component: { control: false },
  }
};

export default meta;
type Story = StoryObj<typeof List>;

export const Basic: Story = {
  tags: ["visual-test"],
  render: (args) => (
    <div className="w-208">
      <List {...args}>
        <ListItem>
          <ListItemText>Default</ListItemText>
        </ListItem>
        <ListItem selected>
          <ListItemText>Selected</ListItemText>
        </ListItem>
        <ListItem disabled>
          <ListItemText>Disabled</ListItemText>
        </ListItem>
        <Divider dense />
        <ListItem>
          <ListItemText>With icon</ListItemText>
          <ListItemIcon>
            <Launch />
          </ListItemIcon>
        </ListItem>
      </List>
    </div>
  ),
};

export const Dark: Story = {
  tags: ["visual-test"],
  ...Basic,
  decorators: [withDarkMode]
};
