import { addons } from "@storybook/manager-api";
import { create } from "@storybook/theming/create";
import packageJson from "../package.json";

addons.setConfig({
  theme: create({
    base: "light",
    colorPrimary: "#002d80",
    brandTitle: `
      <span style="font-size:0.8rem;">Airbus Design System</span><br />
      <span style="font-size:0.8rem;font-weight:normal">Styles (${packageJson.version})<span>`,
    brandUrl: "https://css.design-system.airbus.corp"
  })
});
