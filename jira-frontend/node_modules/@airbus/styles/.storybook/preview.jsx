import React from "react";
import { DocsContainer } from "@storybook/blocks";
import "@airbus/icons/dist/webfont/airbus-icons.css";
import "@fontsource/material-icons";

let defaultBrand;
try {
  defaultBrand = localStorage.getItem("ds-theme");
} catch (err) {}
if (!defaultBrand) defaultBrand = "airbus";

const preview = {
  globalTypes: {
    brand: {
      description: "Choose Brand",
      defaultValue: defaultBrand,
      toolbar: {
        icon: "flag",
        title: "Brand",
        items: [
          { value: "airbus", title: "airbus" },
          { value: "skywise", title: "skywise" },
        ],
        dynamicTitle: true,
      },
    },
  },

  initialGlobals: {
    brand: defaultBrand,
  },

  parameters: {
    viewMode: "docs",
    themes: {
      default: "light",
      list: [
        { name: "light", class: "light-theme", color: "#ffffff" },
        {
          name: "dark",
          class: "dark-theme",
          color: "#000000",
          dataTheme: "dark",
        },
      ],
      target: "html",
      attribute: "data-theme",
    },
    options: {
      storySort: {
        method: "alphabetical",
        order: [
          "General",
          [
            "Welcome",
            "Known Issues",
            "Migration",
            "Visual Styles",
            ["Introduction", "Tokens", "Icons", "Typography"],
            "Layout",
            "Forms",
            ["Introduction", "Form Control", "Examples"],
            "Templates",
          ],
          "Components",
          "Building Blocks",
        ],
      },
    },

    docs: {
      source: { type: "dynamic" },
      toc: { headingSelector: "h2, h3" },
      container: ({ children, context }) => (
        <DocsContainer context={context}>
          <div className="announcement">
            <a href="https://github.airbus.corp/Airbus/design-system/discussions/2583">
              Version 3.0.0 is stable
            </a>
            ! Upgrade to benefit from the latest improvements. (Previous{" "}
            <a href="https://css.design-system.airbus.corp/v2/">
              v2.x documentation
            </a>
            )
          </div>
          {children}
        </DocsContainer>
      ),
    },
  },

  decorators: [
    (Story, context) => {
      const brand = context.globals.brand;
      return (
        <>
          <Story />
          <link
            id="stylesadgfisadiua"
            rel="stylesheet"
            href={`/${brand}/stylesheet.css`}
          />
        </>
      );
    },
  ],
};

export default preview;
