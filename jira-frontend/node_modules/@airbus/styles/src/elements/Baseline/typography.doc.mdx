import { Canvas, Meta, Primary, Source, Story } from "@storybook/blocks";
import LinkTo from "@storybook/addon-links/react";
import * as TypographyStories from "./typography.stories";

<Meta of={TypographyStories} />

# Typography

Used to tell stories, typography are also used for organizing the content, creating hierarchy and ease the end user’s experience.
See [Typography guidelines<i class="mat-icon">launch</i>](https://design-system.airbus.corp/visual-styles/typography/)

<Canvas of={TypographyStories.Basic} />

## Examples

### Body

Default body text styles are included in the <LinkTo kind="General/Visual Styles/Baseline" story="docs">Baseline</LinkTo>.
You can also use the `ds-text--body` class anywhere.

<Canvas of={TypographyStories.Body} />

### Dark mode

<Canvas of={TypographyStories.Dark} />

### Monospace

Alternative font family that can be used for conveying system messages or to ease scannability in specific use cases.

<Canvas of={TypographyStories.Monospace} />
