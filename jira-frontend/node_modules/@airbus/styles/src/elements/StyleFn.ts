import { ThemeTokens } from "../theming";

/**
 * @theme theme object coming from @airbus/design-tokens
 * @mode light or dark mode
 */
export type StyleFn = (theme: ThemeTokens, mode: "light" | "dark") => any;
