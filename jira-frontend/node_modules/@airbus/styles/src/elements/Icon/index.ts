import { StyleFn } from "..";

export const Icon: StyleFn = (theme, mode) => {
  const {
    size: { spacing },
  } = theme;

  return {
    root: {
      margin: 0,
      display: "inline-flex",
      color: "currentColor",
      fill: "currentColor",
      width: spacing[24],
      height: spacing[24],
      fontSize: spacing[24],
      lineHeight: spacing[24],
      cursor: "pointer",
      pointerEvents: "inherit",
    },
    // default size
    _medium: {},
    _xxsmall: {
      width: spacing[12],
      height: spacing[12],
      fontSize: spacing[12],
      lineHeight: spacing[12],
    },
    _xsmall: {
      width: spacing[16],
      height: spacing[16],
      fontSize: spacing[16],
      lineHeight: spacing[16],
    },
    _small: {
      width: spacing[20],
      height: spacing[20],
      fontSize: spacing[20],
      lineHeight: spacing[20],
    },
    _large: {
      width: spacing[32],
      height: spacing[32],
      fontSize: spacing[32],
      lineHeight: spacing[32],
    },
    _xlarge: {
      width: spacing[40],
      height: spacing[40],
      fontSize: spacing[40],
      lineHeight: spacing[40],
    },
  };
};
