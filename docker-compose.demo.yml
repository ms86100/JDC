networks:
  jira-network:
    driver: bridge

volumes:
  postgres_data: {}

services:
  postgres:
    image: postgres:16-alpine
    container_name: jira-postgres
    environment:
      POSTGRES_USER: jiraadmin
      POSTGRES_PASSWORD: jirapass123
      POSTGRES_DB: jira_platform
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - jira-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jiraadmin -d jira_platform"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: jira-redis
    ports:
      - "6379:6379"
    networks:
      - jira-network
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  gateway:
    image: ghcr.io/ms86100/jira-gateway:latest
    container_name: jira-gateway
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: jira-platform-super-secret-key-that-is-at-least-256-bits-long
    networks:
      - jira-network
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  auth-service:
    image: ghcr.io/ms86100/jira-auth-service:latest
    container_name: jira-auth-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: jira_platform
      DB_USERNAME: jiraadmin
      DB_PASSWORD: jirapass123
      JWT_SECRET: jira-platform-super-secret-key-that-is-at-least-256-bits-long
    networks:
      - jira-network
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: ghcr.io/ms86100/jira-frontend:latest
    container_name: jira-frontend
    ports:
      - "3000:3000"
    networks:
      - jira-network
    depends_on:
      gateway:
        condition: service_started

  project-service:
    image: ghcr.io/ms86100/jira-project-service:latest
    container_name: jira-project-service
    ports:
      - "8083:8083"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: jira_platform
      DB_USERNAME: jiraadmin
      DB_PASSWORD: jirapass123
      JWT_SECRET: jira-platform-super-secret-key-that-is-at-least-256-bits-long
    networks:
      - jira-network
    depends_on:
      postgres:
        condition: service_healthy

  issue-service:
    image: ghcr.io/ms86100/jira-issue-service:latest
    container_name: jira-issue-service
    ports:
      - "8084:8084"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: jira_platform
      DB_USERNAME: jiraadmin
      DB_PASSWORD: jirapass123
      JWT_SECRET: jira-platform-super-secret-key-that-is-at-least-256-bits-long
      EUREKA_CLIENT_ENABLED: "false"
      SPRING_FLYWAY_ENABLED: "true"
      SPRING_FLYWAY_BASELINE-ON-MIGRATE: "true"
      SPRING_FLYWAY_DEFAULT_SCHEMA: "jira_issue"
      SPRING_FLYWAY_SCHEMAS: "jira_issue"
      SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA: "jira_issue"
      SPRING_KAFKA_BOOTSTRAP_SERVERS: "localhost:9092"
      SPRING_DATA_REDIS_HOST: "redis"
    networks:
      - jira-network
    depends_on:
      postgres:
        condition: service_healthy