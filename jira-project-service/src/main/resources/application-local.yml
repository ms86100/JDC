spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jira_platform
    username: jiraadmin
    password: UNIpay@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  flyway:
    enabled: false

server:
  port: 8083

issue:
  service:
    url: http://localhost:8084

logging:
  level:
    com.jira.project: INFO
    org.springframework.web: WARN