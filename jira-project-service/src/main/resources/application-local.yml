spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jira_platform
    username: jiraadmin
    password: ${DB_PASSWORD:UNIpay@123}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true
    schemas: jira_project
    default-schema: jira_project
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8083

issue:
  service:
    url: http://localhost:8084

logging:
  level:
    com.jira.project: INFO
    org.springframework.web: WARN