# Enterprise Configuration
# Based on dc-app-performance-toolkit analysis

version: '3.8'

services:
  # ============ GATEWAY ============
  jira-gateway:
    build: ./jira-gateway
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
      - EUREKA_CLIENT_ENABLED=false
      - KUBERNETES_ENABLED=true
    depends_on:
      - jira-auth
      - jira-issue
      - jira-project
    networks:
      - jira-network

  # ============ CORE SERVICES ============
  jira-auth:
    build: ./jira-auth-service
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
      - SPRING_REDIS_HOST=jira-redis
    depends_on:
      - jira-redis
      - jira-postgres
    networks:
      - jira-network

  jira-issue:
    build: ./jira-issue-service
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
      - SPRING_REDIS_HOST=jira-redis
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=jira-kafka:9092
    depends_on:
      - jira-redis
      - jira-postgres
      - jira-kafka
      - jira-search
    networks:
      - jira-network

  jira-project:
    build: ./jira-project-service
    ports:
      - "8083:8083"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
    networks:
      - jira-network

  jira-workflow:
    build: ./jira-workflow-service
    ports:
      - "8084:8084"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
      - jira-kafka
    networks:
      - jira-network

  jira-user:
    build: ./jira-user-service
    ports:
      - "8085:8085"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
      - jira-redis
    networks:
      - jira-network

  jira-board:
    build: ./jira-sprint-service
    ports:
      - "8086:8086"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
      - SPRING_REDIS_HOST=jira-redis
    depends_on:
      - jira-redis
      - jira-postgres
      - jira-issue
    networks:
      - jira-network

  jira-search:
    build: ./jira-search-service
    ports:
      - "8087:8087"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
      - OPENSEARCH_HOSTS=jira-opensearch:9200
    depends_on:
      - jira-opensearch
      - jira-kafka
    networks:
      - jira-network

  jira-notification:
    build: ./jira-notification-service
    ports:
      - "8088:8088"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-kafka
    networks:
      - jira-network

  jira-audit:
    build: ./jira-audit-service
    ports:
      - "8089:8089"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
      - jira-kafka
    networks:
      - jira-network

  jira-plan:
    build: ./jira-plan-service
    ports:
      - "8090:8090"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
    networks:
      - jira-network

  jira-admin:
    build: ./jira-admin-service
    ports:
      - "8091:8091"
    environment:
      - SPRING_PROFILES_ACTIVE=enterprise
    depends_on:
      - jira-postgres
    networks:
      - jira-network

  # ============ DATA STORES ============
  jira-postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=jira
      - POSTGRES_PASSWORD=jira123
      - POSTGRES_DB=jira_enterprise
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: >
      postgres
      -c max_connections=200
      -c shared_buffers=512MB
      -c effective_cache_size=2GB
      -c maintenance_work_mem=256MB
      -c checkpoint_completion_target=0.9
      -c wal_buffers=16MB
      -c default_statistics_target=100
    networks:
      - jira-network

  jira-redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: >
      redis-server
      --maxmemory 2gb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec
    volumes:
      - redis-data:/data
    networks:
      - jira-network

  jira-redis-cluster:
    image: redis:7-alpine
    ports:
      - "7380-7385:7380-7385"
    command: >
      redis-server --cluster-enabled yes
      --cluster-config-file nodes.conf
      --cluster-node-timeout 5000
    volumes:
      - redis-cluster-data:/data
    networks:
      - jira-network

  jira-opensearch:
    image: opensearchproject/opensearch:2.12.0
    ports:
      - "9200:9200"
      - "9600:9600"
    environment:
      - OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g
      - DISABLE_SECURITY_PLUGIN=true
      - cluster.name=jira-cluster
      - node.name=node-1
      - discovery.seed_hosts=opensearch-node-1,opensearch-node-2
      - bootstrap.memory_lock=true
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    networks:
      - jira-network

  jira-kafka:
    image: confluentinc/cp-kafka:7.5.0
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_ZOOKEEPER_CONNECT=jira-zookeeper:2181
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://jira-kafka:9092
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
      - KAFKA_NUM_PARTITIONS=10
      - KAFKA_LOG_RETENTION_HOURS=168
    depends_on:
      - jira-zookeeper
    networks:
      - jira-network

  jira-zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    ports:
      - "2181:2181"
    environment:
      - ZOOKEEPER_CLIENT_PORT=2181
      - ZOOKEEPER_TICK_TIME=2000
    networks:
      - jira-network

  # ============ MONITORING ============
  prometheus:
    image: prom/prometheus:v2.47.0
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
    networks:
      - jira-network

  grafana:
    image: grafana/grafana:10.2.0
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - jira-network

  jaeger:
    image: jaegertracing/all-in-one:1.50
    ports:
      - "16686:16686"
      - "14268:14268"
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    networks:
      - jira-network

networks:
  jira-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  redis-cluster-data:
  opensearch-data:
  prometheus-data:
  grafana-data: