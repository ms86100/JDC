spring:
  config:
    activate:
      on-profile: external
  datasource:
    url: jdbc:postgresql://in0-eplmdb-v01:5432/systems
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: jira_component
  flyway:
    enabled: true
    baseline-on-migrate: true
    default-schema: jira_component
    schemas: jira_component
    create-schemas: true
    validate-on-migrate: false
    locations: filesystem:migrations-external/jira-component-service

logging:
  level:
    com.jira.component: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
