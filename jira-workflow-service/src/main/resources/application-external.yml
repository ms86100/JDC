spring:
  config:
    activate:
      on-profile: external
  datasource:
    url: jdbc:postgresql://in0-eplmdb-v01:5432/systems
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: jira_workflow
  flyway:
    enabled: true
    baseline-on-migrate: true
    default-schema: jira_workflow
    schemas: jira_workflow
    create-schemas: true
    validate-on-migrate: false
    locations: filesystem:migrations-external/jira-workflow-service

logging:
  level:
    com.jira.workflow: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
