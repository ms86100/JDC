spring:
  config:
    activate:
      on-profile: external
  datasource:
    url: jdbc:postgresql://in0-eplmdb-v01:5432/systems
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: jira_migration
  flyway:
    enabled: true
    baseline-on-migrate: true
    default-schema: jira_migration
    schemas: jira_migration
    create-schemas: true
    validate-on-migrate: false
    locations: filesystem:migrations-external/avionics-systems-migration-service

logging:
  level:
    com.avionics_systems.migration: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
