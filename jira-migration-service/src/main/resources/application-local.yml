spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jira_platform
    username: jiraadmin
    password: UNIpay@123
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false

management:
  endpoint:
    health:
      show-details: always